# LeetCode for Zed

Work in progress.

## Roadmap

- [ ] Prebuilt release binaries, so the extension's auto-download path goes live
- [ ] Publication to the Zed extension registry
- [ ] Progress sessions, which LeetCode exposes only through a legacy endpoint
- [ ] Company tag filtering in `list`, which needs a premium account to verify
