# zeek-zed
Syntax support for Zeek for Zed

```
zeek-zed/
├── extension.toml          # Main extension manifest
└── languages/
    └── zeek/
        ├── config.toml     # Language definition (file extensions, grammar name)
        └── highlights.scm  # The syntax highlighting queries (maps grammar to colors)
```
