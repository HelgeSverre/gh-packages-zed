# orchis-dark-zed-theme
A clean theme based off the colors of Orchis Dark

## Install

To install, copy the directory `orchis-dark-theme` to the
respective extensions directory.

Example: `~/.local/share/zed/extensions/installed` on **Linux**
